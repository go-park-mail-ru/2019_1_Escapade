package engine

/*
import (
	"testing"

	"github.com/go-park-mail-ru/2019_1_Escapade/internal/pkg/models"
	"github.com/go-park-mail-ru/2019_1_Escapade/internal/pkg/utils"
	. "github.com/smartystreets/goconvey/convey"
)

var correctSettings = &models.RoomSettings{
	Width:         7,
	Height:        7,
	Players:       2,
	Observers:     10,
	TimeToPrepare: 5,
	TimeToPlay:    60,
	Mines:         2,
}

// unit
func TestCharacteristicsCheckNameLen(t *testing.T) {

	// Only pass t into top-level Convey calls
	Convey("Given invalid room settings: namelen < constants.ROOM.NameMin", t, func() {
		rs := correctSettings
		rs.Name = ""
		for i:=0; i <

		Convey("When we use CharacteristicsCheck to verify room setiings", func() {
			result := CharacteristicsCheck(rs)
			Convey("Then the result will be false", func() {
				So(result, ShouldBeFalse)
			})
		})
	})
}*/
